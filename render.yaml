services:
  - type: web
    name: ctfd
    env: docker
    repo: https://github.com/YOUR_USERNAME/CTFd
    branch: main
    plan: free
    dockerfilePath: Dockerfile
    buildCommand: docker-compose build
    startCommand: docker-compose up
