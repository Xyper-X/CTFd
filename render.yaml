services:
  - type: web
    name: ctfd
    env: docker
    repo: https://github.com/Xyper-X/CTFd
    branch: main
    plan: free
    dockerfilePath: Dockerfile
    buildCommand: docker-compose build
    startCommand: docker-compose up
